export declare class AssignTicketCommand {
    readonly ticketId: number;
    readonly assigneeId: number;
    readonly actorRole: string;
    constructor(ticketId: number, assigneeId: number, actorRole: string);
}
